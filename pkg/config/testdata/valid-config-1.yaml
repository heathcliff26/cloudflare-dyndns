logLevel: "info"

server:
  port: 8080
  domains:
    - example.org
    - example.net

client:
  token: "test-token-1"
  proxy: true
  domains:
    - foo.example.org
  interval: "5m"
  endpoint: "dyndns.example.org"

metrics:
  enabled: true
  port: 12345
  goCollector: false
  processCollector: false
