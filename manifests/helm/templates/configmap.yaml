apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudflare-dyndns.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cloudflare-dyndns.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{- .Values.config | toYaml | nindent 4 }}
