logLevel: "info"

server:
  port: 8080
  domains:
    - example.org
    - example.net

client:
  token: "test-token-1"
  proxy: true
  domains:
    - foo.example.org
  interval: 300
  endpoint: "dyndns.example.org"
